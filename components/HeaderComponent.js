import { Button, View } from "react-native"

const HeaderComponent = (props) => {
    return (
        <View>
            <Button title="Next Screen" color="white" />
        </View>
    )
}
export default HeaderComponent